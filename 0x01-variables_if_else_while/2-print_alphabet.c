#include <stdlib.h>
#include <time.h>
/* more headers goes there */

/* to print the lowercase alphabet*/

int main(void)
{
	char n;


	for(n='a'; i<='z'; i++);
	putchar(ch);
	putchar('\n');

	return (0);
}
